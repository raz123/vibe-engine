# Journal — 2026-05-26

## Project: Vibe Engine (AI DJ)

### GitHub
- Repo: https://github.com/raz123/vibe-engine
- Remote: origin (HTTPS)

### Environment
- OS: Windows
- Git: `C:\Program Files\Git\bin\git.exe` (not in PATH)
- Git user: raz123 / redag2@gmail.com
- Credentials: Windows Credential Manager (git credential-manager)
- Node/npm: `C:\Program Files\nodejs` (needs PATH prepend)
- Python: system default
- Working dir: `C:\Users\tower\Documents\vibe_coding_opencode\may26`

### Architecture Decisions (MVP)
- **Frontend**: React + Electron + Tailwind + Zustand
- **Backend**: Python + FastAPI
- **Database**: PostgreSQL (Phase 1 local, Phase 2+ cloud)
- **Cache**: Redis (optional, for queues/cache)
- **GPU**: CUDA-capable for Demucs/ML inference

### Audio Pipeline
- yt-dlp + FFmpeg for extraction
- librosa + Essentia for analysis (BPM, key, structure, energy)
- Demucs for stem separation
- Rubber Band Library + FFmpeg for real-time DSP

### Transition Engine (heart of system)
6 transition types: Simple Crossfade, EQ Blend, Drum Carry, Vocal Swap, Drop Transition, Echo/Reverb Exit
Hard constraints: no key clashes, no vocal overlap, no off-beat cuts, no mid-phrase cuts

### Recommendation Engine
Track graph model with energy/mood/BPM/key/genre embeddings
Session phases: warmup -> buildup -> peak -> sustained high -> cooldown
Feedback loop: reinforcement scoring on skips/likes/duration

### Vibe Modes
Club, Chill, Focus, Late Night, Aggressive, Euphoric, Deep House, Experimental

### Milestones
1. Basic playback + crossfade
2. BPM + key-aware transitions
3. Phrase-aware transitions
4. Stem-separated smart transitions
5. AI emotional queue planning

### Legal
- No permanent hosting of copyrighted music
- No redistribution
- Temporary local caching only (LRU, auto-delete, configurable cap)
- User-supplied playback sources only
- All processing client-side or ephemeral

### Design Philosophy
Not a playlist player — an autonomous vibe engine.
Transition quality > UI. Song selection > transition effects.
Avoid dead air. Avoid vocal collisions. Prioritize emotional continuity.

---

## Build Session — MVP Complete (2026-05-26)

### What was built

**Backend (Python/FastAPI):**
- `app/main.py` — FastAPI server with 15 REST endpoints
- `app/audio/extractor.py` — yt-dlp + FFmpeg audio download pipeline
- `app/audio/analyzer.py` — librosa analysis: BPM, key (Camelot), energy, mood classification, structure detection
- `app/audio/transition.py` — Transition scoring engine (6 types: crossfade, EQ blend, drum carry, vocal swap, drop transition, echo exit)
- `app/services/queue.py` — Queue planner with session state, vibe modes, optimization
- `app/models/schemas.py` — Pydantic models for all API types

**Frontend (React + Vite + Tailwind):**
- `src/components/Player.jsx` — Audio player with play/pause/skip, seek bar, transition info
- `src/components/ImportBar.jsx` — YouTube URL import with loading state
- `src/components/Queue.jsx` — Track queue with analysis badges
- `src/components/VibeControl.jsx` — 8 vibe mode selector
- `src/components/Visualizer.jsx` — Equalizer animation
- `src/stores/useStore.js` — Zustand store with all API integration
- `public/electron.js` — Electron shell

**Integration:**
- Backend serves built frontend as static files (single port)
- `start.bat` — one-click launcher

### Verified working
1. Backend health check
2. Frontend served (React app loads)
3. Static assets (CSS/JS served correctly)
4. YouTube import (download + analyze in ~11s)
5. Queue management
6. Transition planning (picks `drop_transition` for compatible tracks)
7. Audio streaming (46MB WAV, proper MIME type)
8. Vibe mode switching (all 8 modes)
9. LRU cache with stats
10. Full end-to-end integration

### Test results
```
Track: Ed Sheeran - Shape of You
BPM: 129.2 | Key: F#m (11A) | Energy: 1.00
Mood: hype, aggressive
Transition: drop_transition (4.0s crossfade)
```

### Environment quirks
- `&&` doesn't work in PowerShell — use `; if ($?) { }`
- `where` doesn't find system-installed Python even though `python --version` works
- `Start-Process` gets killed by bash tool — use `subprocess.Popen` for background processes
- `winget` msstore source blocks installs — use `--source winget`
- Books 0.11 returns tempo as array — needed `hasattr(tempo, "__iter__")` guard

### Architecture choices made
- **Single-server deployment**: Backend serves built frontend (no CORS, no separate port)
- **No database yet**: SQLite/PostgreSQL deferred to Phase 2; in-memory session + JSON file cache for now
- **No GPU/Demucs yet**: Async stem separation pipeline deferred to Phase 2
- **Rule-based mood classifier**: ML models deferred; current approach fast and good enough for MVP
